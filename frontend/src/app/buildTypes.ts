export class BuildTypes {
    buildTypeID: number;
    buildType: string

constructor(
    buildTypeID: number,
    buildType: string
){
    this.buildTypeID = buildTypeID
    this.buildType = buildType
}}
